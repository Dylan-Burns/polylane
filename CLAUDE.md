# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Project

polylane is an agent for Cloudflare Workers. The repository is currently a blank slate — it contains only this file and a README; no code, build tooling, or tests exist yet.

## Status

When scaffolding begins (e.g. `wrangler init` / a Workers project with `wrangler.jsonc`), update this file with:

- Build, dev, deploy, and test commands (including how to run a single test)
- The architecture that emerges (entry points, bindings, durable objects, agent loop structure)
